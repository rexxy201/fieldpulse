node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Real secrets — lives only on the live server, never committed.
secrets.php

# Local file uploads and test artifacts — never committed.
uploads/
.phpunit.cache/